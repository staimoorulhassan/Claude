{
  "name": "vapi-control",
  "version": "1.0.0",
  "description": "Deploy and hot-swap the Vapi pharmacy lead-gen assistant (provider/voice/speed/verifier) via API or webhook.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "deploy": "node src/deploy.js",
    "start": "node src/server.js",
    "render": "node src/deploy.js --dry-run"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
