[general]
enabled=yes
port=5038
bindaddr=0.0.0.0

; avr-ami connects with this user. Change the password and match AMI_PASSWORD in config/.env.
[avr]
secret=changeme_match_AMI_PASSWORD
permit=0.0.0.0/0.0.0.0
read=all
write=all
