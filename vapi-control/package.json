{
  "name": "vapi-control",
  "version": "1.0.0",
  "description": "Vapi assistant deploy + provider hot-swap + lead-capture webhook for pharmacy lead-gen campaign",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "deploy": "node src/deploy.js",
    "render": "node src/deploy.js --dry-run",
    "build": "echo 'no build step needed'"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
